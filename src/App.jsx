/**
 * App - root component that renders the Home page (Expense Tracker).
 * No routing; single-page application.
 */

import Home from './pages/Home';

function App() {
  return <Home />;
}

export default App;
